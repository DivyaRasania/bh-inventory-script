# Arch script to get information about the device for inventory in Binary Hearts

## How to run
Boot into Arch live environment(archiso) and run this command
```bash
curl -sL https://bit.ly/get-stats | bash
```
